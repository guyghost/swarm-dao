---
"@guyghost/swarm-dao-graph": minor
---

Add `@guyghost/swarm-dao-graph`: the Graph Engineering run executor (journal-replayed runs, strict signal validation, `runGraphCli` entry) previously repo-local under `tools/graph-engineering`. Behavior is unchanged; `tools/graph-engineering/*` now re-export from the package.
