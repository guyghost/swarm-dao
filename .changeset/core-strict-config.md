---
"@guyghost/swarm-dao-core": minor
---

Strict validated loadConfig: ENOENT returns defaults, invalid JSON/enums throw with path. Validates mode, execution.isolation, deliberation, ship, delegation, herdr/tmux timeouts with bounds. Deep-merges nested objects instead of shallow overwrite. Adds ProjectConfig.delegation for README parity.
