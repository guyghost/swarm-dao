---
"@guyghost/swarm-dao-core": minor
---

Add a TypeSafe-style classifier verdict: coding-loop workers emit closed-vocabulary JSON, the harness validates it without throwing, and evaluateAttempt composes that signal with tool evidence so done cannot skip tests or Graph evaluation.
