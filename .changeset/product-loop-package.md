---
"@guyghost/swarm-dao-product": minor
---

Add `@guyghost/swarm-dao-product`: the product-loop run executor (journal-replayed runs, producer-bound signal validation, `runProductCli` entry) previously repo-local under `tools/product-loop`. Behavior is unchanged; `tools/product-loop/*` now re-export from the package.
