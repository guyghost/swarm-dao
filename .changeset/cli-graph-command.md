---
"@guyghost/swarm-dao-cli": minor
---

Add `swarm-dao graph <init|status|submit>` — Graph Engineering change-control runs in any project (evidence under `.dao/graph-runs` by default), alongside the existing `improve` loop commands.
