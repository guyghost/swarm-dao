---
description: Advance an improvement series by one state-authorized effect (deterministic executor; persisted worker configuration, per-series worktree)
allowed-tools: mcp__swarm-dao__dao_improve_once
---

Advance an improvement series by one state-authorized effect (deterministic executor; persisted worker configuration, per-series worktree).

`/dao:improve-once seriesId [evidenceRoot] [cycleRoot]`

Call the `mcp__swarm-dao__dao_improve_once` MCP tool. Pass `$ARGUMENTS` through.
