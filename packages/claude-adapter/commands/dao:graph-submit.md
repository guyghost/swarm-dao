---
description: Submit an AI-source signal to a Graph Engineering run (model/implementation artifacts only; human events go through the CLI)
allowed-tools: mcp__swarm-dao__dao_graph_submit
---

Submit an AI-source signal to a Graph Engineering run (model/implementation artifacts only; human events go through the CLI).

`/dao:graph-submit runId type producer payload evidence`

Call the `mcp__swarm-dao__dao_graph_submit` MCP tool. Pass `$ARGUMENTS` through.
