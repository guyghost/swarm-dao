---
description: Read a Graph Engineering run snapshot (read-only)
allowed-tools: mcp__swarm-dao__dao_graph_status
---

Read a Graph Engineering run snapshot (read-only).

`/dao:graph-status runId [evidenceRoot]`

Call the `mcp__swarm-dao__dao_graph_status` MCP tool. Pass `$ARGUMENTS` through.
