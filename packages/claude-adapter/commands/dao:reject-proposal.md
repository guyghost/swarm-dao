---
description: Reject a proposal with an auditable human reason
allowed-tools: mcp__swarm-dao__dao_reject
---

Reject a proposal with an auditable human reason.

`/dao:reject-proposal proposalId --reason <text>`

Call the `mcp__swarm-dao__dao_reject` MCP tool. Pass `$ARGUMENTS` through.
