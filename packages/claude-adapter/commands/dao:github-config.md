---
description: Configure the GitHub integration (auth via the gh CLI)
allowed-tools: mcp__swarm-dao__dao_config_github
---

Configure the GitHub integration (auth via the gh CLI).

`/dao:github-config --owner <o> --repo <r> [--issues]`

Call the `mcp__swarm-dao__dao_config_github` MCP tool. Pass `$ARGUMENTS` through.
